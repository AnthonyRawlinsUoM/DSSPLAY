import { DfConsumerDirective } from './df-consumer.directive';

describe('DfConsumerDirective', () => {
  it('should create an instance', () => {
    const directive = new DfConsumerDirective();
    expect(directive).toBeTruthy();
  });
});
