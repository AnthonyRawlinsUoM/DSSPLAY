import { ExplainDirective } from './explain.directive';

describe('ExplainDirective', () => {
  it('should create an instance', () => {
    const directive = new ExplainDirective();
    expect(directive).toBeTruthy();
  });
});
