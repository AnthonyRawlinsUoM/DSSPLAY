import { CollectorDirective } from './collector.directive';

describe('CollectorDirective', () => {
  it('should create an instance', () => {
    const directive = new CollectorDirective();
    expect(directive).toBeTruthy();
  });
});
