import { DockableDirective } from './dockable.directive';

describe('DockableDirective', () => {
  it('should create an instance', () => {
    const directive = new DockableDirective();
    expect(directive).toBeTruthy();
  });
});
